let duration = getSelectedOption(durationOptions)
let genre = getSelectedOption(textTypeOptions)
let file = getSelectedOption(textFilesOptions)
let forceCorrection = false
const stateOfApplication = {
  playing: false,
  started: false,
}
